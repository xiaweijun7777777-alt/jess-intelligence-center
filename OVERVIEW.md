# Jess' Intelligence Center · 智能机器人法规情报中枢 — 概览

> EVA / NERV 视觉风格 · 自包含静态站（HTML + CSS + 原生 JS，零依赖）。数据存于浏览器 localStorage，不上传。

## 设计风格
- **EVA / NERV 风格**：深空近黑底 + NERV 紫 / 青绿强调色、MAGI 终端绿扫描线、黑黄危险条纹（Geofront 顶栏 / 更新周期框）、HUD 角标框、压缩等宽字体（Oswald / Rajdhani / Share Tech Mono）；侧栏点缀 "God's in his heaven…" 经典铭文。
- **EVA 原画背景**：使用用户指定的 EVA 原画作为全屏背景（`assets/bg/bg1.jpg`），通过暗色渐变遮罩保证文字可读；亮色主题下自动加深遮罩。
- **工作台头像**：侧栏 logo 使用用户指定的 EVA 初号机极简壁纸（`assets/avatar/avatar.jpg`），圆形 HUD 边框 + 辉光呼吸动画。
- **每板块强调色自动跟随**：进入板块时整套强调色切换为该板块色——新法动态=青、合规更新=紫、行业动态=绿、地缘政治=品红、处罚案例=红橙；返回总览/检索/收藏恢复默认 NERV 紫青。
- 保留 暗 / 亮 / 跟随系统 三态主题切换（默认暗色）。

## 板块结构（导航顺序）
1. **主控台**（主页）— 聚合各板块计数、待确认项、收藏数与跨板块最新动态流。
2. **新法动态**（原「法规收集」）— 智能机器人行业相关法规（广告/产品/售后/消费者/数据/进出口…），**来源类型仅限官方发布**。
3. **合规更新** — 监管指引、机构解读、律所文章、新闻媒体等；来源不限于官方。
4. **行业动态** — 科技型企业动态均可，不限于扫地机产品。
5. **地缘政治** — 出口管制、制裁、贸易战、供应链、数据主权等（含「影响等级」标签：严重/中等/轻微）。
6. **处罚案例** — 反垄断、反倾销/反补贴、出口管制、数据违规、税务等科技型企业重要案件。
7. **收藏** — 跨板块聚合你标记「☆ 收藏」的事项，可按板块筛选，方便随时查看关注项。
8. **实时检索** — 关键词即时检索本库 + 联网调取维基百科等公开资料。

## 关键能力
- **实时刷新按钮**：顶部栏「↻ 刷新」一键打开实时刷新面板，输入关键词后同时检索本库与维基百科（中/英），可按板块范围 / 仅官方来源筛选，并支持一键必应联网检索兜底。
- **收藏区**：每张卡片与详情弹窗均有「☆ 收藏」按钮，一键加入收藏；收藏状态在 localStorage 持久化，左侧导航「收藏」入口按板块筛选聚合展示。
- **待确认 / 已核实 核实机制**：每条信息默认「待确认」，卡片与详情均可一键切换；可按核实状态筛选，主页/导航显示待确认与收藏计数，方便你亲自核对。
- **官方第一梯队呈现**：所有列表按「官方来源优先 → 时间倒序」排序；新法动态强制官方来源。
- **实时检索**：输入即检索本库全量情报；同时自动联网抓取维基百科（中/英）实时结果，并支持一键「必应联网检索」跳转。
- **信息更新周期：每日 09:00**：侧栏固定显示「情报更新周期 每日 09:00」与实时倒计时；已创建每日 09:00 自动化任务，生成晨间情报简报（仅检索整理，不改本地文件）。
- 详情弹窗（原文跳转 / 编辑 / 删除 / 收藏）、schema 驱动添加/编辑表单、JSON 导入导出、本地持久化。

## 数据说明
- `assets/data.js` 已填充**真实权威数据**：欧盟 AI Act / 机械法规（EUR-Lex）、美国 NIST AI RMF 与生成式 AI 概要（NIST 官方 PDF）、中国工信部人形机器人指导意见与两用物项出口管制条例、欧盟对华电动车反补贴、Google/Apple 反垄断与税务案、BIS/Haas 出口管制和解、舟山海关处罚等。
- 部分官方采用机构主域名（深链路径待你核实后替换）；**所有条目默认「待确认」，请你亲自核验原文**。

## 文件结构
```
regulation-workbench/
├─ index.html          EVA/NERV 布局 + 收藏视图 + 实时检索视图 + 实时刷新面板
├─ assets/
│  ├─ styles.css       EVA / NERV 设计系统（含 EVA 原画背景、头像、刷新面板样式）
│  ├─ app.js           导航 + 收藏 + schema 驱动渲染/筛选/核实/实时检索/实时刷新/每日更新/持久化
│  ├─ data.js          五大板块真实数据（BOARD_DATA）
│  ├─ avatar/
│  │  └─ avatar.jpg    用户指定的 EVA 初号机极简壁纸（工作台头像）
│  └─ bg/
│     ├─ bg1.jpg … bg7.jpg  用户指定的 EVA 原画背景
└─ OVERVIEW.md
```

## 预览方式
- 直接双击 `index.html`（普通 `<script>`，file:// 可用）；
- 或在工作台内打开预览（推荐，可正确加载相对资源与联网检索）。

## 下一步可优化
1. 把示例/主域链接替换为逐条核实后的深链原文。
2. 增加时间轴视图、法规地图、变更高亮（新增 vs 历史）。
3. 总览加趋势图（按时间/国家分布）。
4. 如需协作与云端同步，可平滑升级到 Laravel + Livewire + FluxUI。
